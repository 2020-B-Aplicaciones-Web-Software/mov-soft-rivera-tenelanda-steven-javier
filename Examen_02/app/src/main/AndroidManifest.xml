<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.examen_02">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Examen_02">
        <activity android:name=".ActualizarRecetaMedica"></activity>
        <activity android:name=".ActualizarMedicamento" />
        <activity android:name=".CrearMedicamento" />
        <activity android:name=".CrearRecetaMedica" />
        <activity android:name=".MostrarMedicamento" />
        <activity android:name=".MostrarRecetaMedica" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>